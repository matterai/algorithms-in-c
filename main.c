#include "io.h"
#include "sortings.h"

#define ARRAY_SIZE 7

static int arr [ARRAY_SIZE] = { 5, 7, 1, 2, 9, 4, 0 };

int main(int argc, char const *argv[])
{
    print_array(arr, ARRAY_SIZE);
//    printf("fuck");

    return 0;
}
